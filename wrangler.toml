name = "santri-dana-ku-api"
main = "src/index.ts"
compatibility_date = "2024-04-01"

[vars]
# Add non-secret vars here if needed
# DATABASE_URL and JWT_SECRET should be added via 'wrangler secret put'

[dev]
port = 3001
